<% title "Blog" %>

<% if can? :create, PhotoAlbum %>
  <%= render partial: 'pages/page_action', locals: { link: new_post_path, icon: 'plus' } %>
<% end %>

<% content_for :back_link do %>
  <%= root_path %>
<% end %>

<div class="row">
  <div class="columns">
    <%= will_paginate @posts, page_links: false %>
    <%= render 'posts_list' %>
    <%= will_paginate @posts, page_links: false %>
  </div>
</div>
