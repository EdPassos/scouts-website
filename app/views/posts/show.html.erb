<div class='content'>
	<div class="row">
		<div class="columns">
			<h1><%= title @post.title %></h1>
		</div>
	</div>
	<div class="row">
		<div class="columns">
			<%= simple_format @post.body %>
			<small><%= "Criado a #{@post.created_at.strftime('%-d/%m/%y')}" %></small>
		</div>
	</div>
</div>
