<%= form_for @post do |f| %>
	<%= f.label :title, "Título" %>
	<%= f.text_field :title %>
	<%= f.label :body, "Texto" %>
	<%= f.text_area :body, rows: 15 %>
	<%= f.submit class: "button" %>
<% end %>
