<%= form_for @user do |f| %>
	<div class="row collapse">
		<div class="columns small-3 large-2">
			<%= f.label :email, class: "prefix" %>
		</div>
		<div class="columns small-9 large-10">
			<%= f.email_field :email, autofocus: true %>
		</div>
	</div>

	<div class="row collapse">
		<div class="columns small-3 large-2">
			<%= f.label :password, class: "prefix" %>
		</div>
		<div class="columns small-9 large-10">
			<% if @minimum_password_length %>
				<%= f.password_field :password, autocomplete: "off", placeholder: "Mínimo de #{@minimum_password_length} caracteres" %>
			<% else %>
				<%= f.password_field :password, autocomplete: "off" %>
			<% end %>
		</div>
	</div>

	<div class="row collapse">
		<div class="columns small-3 large-2">
			<%= f.label :password_confirmation, "Password", class: "prefix" %>
		</div>
		<div class="columns small-9 large-10">
			<%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirmar password" %>
		</div>
	</div>

	<div class="row collapse">
		<div class="columns small-9 large-10 small-offset-3 large-offset-2">
			<%= f.submit "Gravar", class: "button expand" %>
		</div>
	</div>

	<% if @user.errors.any? %>
		<div class="row collapse">
			<div class="columns small-12 large-10 large-offset-2">
				<div data-alert class="alert-box alert radius">
					<h2><%= pluralize(@user.errors.count, "erro") %> a gravar o utilizador:</h2>

					<ul>
						<% @user.errors.full_messages.each do |msg| %>
							<li><%= msg %></li>
						<% end %>
					</ul>
					<a href="#" class="close">&times;</a>
				</div>
			</div>
		</div>
	<% end %>
<% end %>
